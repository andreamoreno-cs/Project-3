// replit is weird with having a separate cpp file
// so for now, this stuff will be under graph.h and later well fix

/* 
#include "graph.h"
#include <string>
#include <istream>
#include <algorithm>

using namespace std;

void Graph::addEdge(int u, int v)
{
}
void Graph::addData(string county, string state))
{
    string line, name;
    ifstream file("county_demographics.csv", ios::binary);
    getline(file, line)
}

void Graph::DFS()
{
} */